This Python script generates strong, random passwords based on user-defined criteria, including the inclusion of uppercase letters, lowercase letters, digits, and symbols. Users can specify the length of the password, making it flexible for various security needs.

## Features

- Generates strong passwords with a mix of uppercase, lowercase, digits, and symbols.
- Allows users to specify the desired password length.
- Generates 10 unique passwords per run for variety.

## Getting Started

To get started with this password generator, clone this repository to your local machine and run the file 'PasswordGenerator.py'
